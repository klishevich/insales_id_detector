<h3>Админка: аккаунты</h3>
<div class="span8">
<table class="table table-condensed" style="table-layout : fixed" width="900px">
  <thead>
    <tr>
      <th align="left" width="15%">Дата создания</th>
      <th align="left" width="40%">Сайт</th>
      <th align="left" width="15%">Счетчик установлен</th>
    </tr>
  </thead>
  <tbody>
  <% @accounts.each do |acc| %>
    <tr>
      <td><%= acc.created_at.strftime('%d.%m.%Y %T')  %></td>
      <td><%= acc.insales_subdomain %></td>
      <td><%= acc.has_counter %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
